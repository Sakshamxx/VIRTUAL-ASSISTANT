You are a senior full-stack developer and AI systems architect.

I have an existing Python-based AI voice assistant project called "JARVIS" with the following capabilities:
- Voice recognition using SpeechRecognition
- Text-to-speech using pyttsx3/gTTS
- AI responses via OpenAI/Gemini API
- Music playback via a local dictionary
- News fetching via APIs
- Basic command execution (opening websites, etc.)

Current structure:
- main.py (core logic)
- api_ai.py (AI responses)
- musicLib.py (music mapping)
- .env (API keys)

Your task is to transform this into a scalable, production-ready full-stack application with both backend and frontend.

### 🔧 Backend Requirements:
- Use Python (FastAPI preferred)
- Convert existing logic into RESTful APIs:
  - /voice-command
  - /chat
  - /play-music
  - /news
- Implement WebSocket support for real-time voice interaction
- Modular architecture (controllers, services, routes)
- Proper error handling and logging
- Secure API key management using environment variables
- Add authentication (JWT-based login/signup)
- Ensure scalability and clean code practices

### 🎨 Frontend Requirements:
- Build a modern UI using React (or Next.js)
- Features:
  - Voice input button (mic animation)
  - Chat interface (like ChatGPT)
  - Real-time responses from backend
  - Music player UI
  - News dashboard
- Use clean UI/UX principles (dark theme, minimal, futuristic feel like JARVIS)
- Integrate WebSockets for live communication
- Responsive design (mobile + desktop)

### ⚡ Additional Features:
- Add a dashboard showing:
  - User activity
  - Command history
- Option to switch between text and voice mode
- Add loading animations and feedback states
- Deploy-ready (Docker support preferred)

### 📦 Output Expectations:
- Folder structure for frontend and backend
- Sample API implementations
- Example React components
- Instructions to run locally
- Suggestions for deployment (Vercel + Render / AWS)

Ensure the system is clean, scalable, and follows industry best practices.